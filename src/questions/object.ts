import { test_obj } from '../global-var';

export const object_q1 = () => {
  // insert new key value {favouriteArtist: "Billie Ellish"} into the object
  // delete key "age"

  const obj = test_obj;

  // logic goes here

  return obj;
};
