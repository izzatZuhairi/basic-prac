// basic logic (google: js map function)

import { num_arr } from './global-var';

const map_exercise = () => {
  const arr_compare = [3, 4, 1, 5, 6];

  let less_than_max_num_arr_count: number = 0;
  let more_than_min_num_arr_count: number = 0;

  // guide
};

console.log(!!Number('1'));
